setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Payment/Dockerfile
      env_variables:
        ASPNETCORE_ENVIRONMENT: Production
        DATABASE_URL: $DATABASE_URL
    
      # environment:
      #   ASPNETCORE_ENVIRONMENT: $ASPNETCORE_ENVIRONMENT
      #   DATABASE_URL: $DATABASE_URL

    